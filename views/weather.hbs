
<!DOCTYPE html>
<html>

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-3">
        <div class="mt-4 p-5 bg-primary text-white rounded">
            <h1>Weather Data</h1>
            <p>
                Pack your Bag, According to the weather!!! 
            </p>
        </div>
    </div>
    <div class="container mt-5">
        <div class="container mt-5">
            <div class="mb-3">
                <label for="dateSelect" class="form-label">Choose a Date:</label>
                <form action="">
                    <input type="hidden" name="lat" id="param1" value={{lat}}>
                    <input type="hidden" name="long" id="param2" value={{long}}>
                    <input type="hidden" name="cityName" id="param3" value={{cityName}}>
                    <input type="hidden" name="selectedDate" id="param3" value={{selectedDate}}>
                    <select name="date" class="form-select" id="dateSelect">
                        <option value={{firstDate}}>Select a Date</option>
                        <option value={{firstDate}}>{{firstDate}} and Upcoming</option>
                        <option value={{secondDate}}>{{secondDate}}and Upcoming</option>
                        <option value={{thirdDate}}>{{thirdDate}}and Upcoming</option>
                        <option value={{fourthDate}}>{{fourthDate}}and Upcoming</option>
                        <option value={{fifthDate}}>{{fifthDate}}and Upcoming</option>
                    </select>
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
            </div>
        </div>

        <div>
            <td><h2>City Name</h2></td> 
            <td><h2 style="color: red;">{{cityName}}</h2></td>
        </div>

        {{#each weatherDataToDisplay}}
        <h3 style="color: green;">{{this.dt_txt}}</h3>
        <table class="table table-striped">
            {{#each this.weather}}
            <tr>
                <th>Current Condition</th>
                <td>{{this.main}}</td>
            </tr>
            {{/each}}
            <tr>
                <th>Current Temperature</th>
                <td>{{this.main.temp}} C°</td>
            </tr>
            <tr>
                <th>Feels Like</th>
                <td>{{this.main.feels_like}} C°</td>
            </tr>
            <tr>
                <th>Wind Speed</th>
                <td>{{this.wind.speed}} meter/sec</td>
            </tr>
   
            </tr>
        </table>
        {{/each}}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
</body>

</html>
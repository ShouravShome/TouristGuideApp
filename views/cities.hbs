
<!DOCTYPE html>
<html>

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-5">
        <h1>City Data</h1>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>City Name</th>
                    <th>Population</th>
                    <th>News</th>
                    <th>Weather</th>
 
                </tr>
            </thead>
            <tbody>
                {{#each citiesData}}
                <tr>
                    <td>{{this.name}}</td>
                    <td>{{this.population}}</td>
                    <td><a href="/news?cityName={{this.name}}">News Headlines</a></td>
                    <td><a href="/weather?lat={{this.latitude}}&long={{this.longitude}}&cityName={{this.name}}">Weather Data</a></td>

                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
</body>

</html>